/**
 * FastAPI backend (`backend/`) default — must match `PORT` in `backend/.env`.
 * Express Cloud Run locally often uses 8080; set that explicitly in Settings or EXPO_PUBLIC_API_URL when using `cloud-run/`.
 */
export const DEFAULT_BACKEND_PORT = 8000;
